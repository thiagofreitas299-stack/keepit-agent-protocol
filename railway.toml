[deploy]
startCommand = "pip install -r api/requirements.txt && uvicorn api.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30

[build]
builder = "nixpacks"

[[services]]
name = "keepit-api"
